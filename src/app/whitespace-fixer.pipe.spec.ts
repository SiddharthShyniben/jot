import { WhitespaceFixerPipe } from './whitespace-fixer.pipe';

describe('WhitespaceFixerPipe', () => {
  it('create an instance', () => {
    const pipe = new WhitespaceFixerPipe();
    expect(pipe).toBeTruthy();
  });
});
